- defaults:
    brewfile:
        - stdout: true
    link:
        relink: true

- link:
    ~/.gitconfig: git/redcanary.gitconfig
    ~/Brewfile: brew/redcanary.brewfile

- install-brew: true

- brewfile:
    - brew/redcanary.brewfile
